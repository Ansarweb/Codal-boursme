name: Codal Boursme

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test GitHub Actions
        run: |
          echo "CODAL BOURSME STARTED"
          python --version
